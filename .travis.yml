language: python

python:
  - "2.7"

install:
  - "pip install -r requirements.txt --use-mirrors"
  - "pip install pymongo"
  - "pip install pysqlite sqlalchemy"

services:
  - mongodb

script: nosetests
